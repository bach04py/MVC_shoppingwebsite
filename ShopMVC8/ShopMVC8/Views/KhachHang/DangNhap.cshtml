@model ShopMVC8.ViewModels.LoginVM
@{
    ViewData["Title"] = "Đăng Nhập";
}

<div class="container-fluid py-5">
    <div class="container py-5">
        <h1 class="h5 text-uppercase mb-4">Đăng Nhập</h1>

        <form method="post" asp-action="DangNhap" asp-route-returnurl="@ViewBag.ReturnUrl">
            <div class="text-danger" asp-validation-summary="All"></div>

            <div class="row">
                <div class="col-md-12 col-lg-6">
                    <div class="form-item w-100">
                        <label asp-for="@Model.Username" class="form-label my-3"></label><sup>*</sup>
                        <input asp-for="@Model.Username" class="form-control" />
                        <span asp-validation-for="@Model.Username" class="text-danger"></span>
                    </div>

                    <div class="form-item w-100">
                        <label asp-for="@Model.Password" class="form-label my-3"></label><sup>*</sup>
                        <input asp-for="@Model.Password" class="form-control" />
                        <span asp-validation-for="@Model.Password" class="text-danger"></span>
                    </div>

                    <div class="col-lg-12 form-group">
                        <button type="submit" class="btn btn-dark">Đăng Nhập</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
